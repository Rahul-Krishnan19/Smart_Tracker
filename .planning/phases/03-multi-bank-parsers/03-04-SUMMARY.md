---
phase: 03-multi-bank-parsers
plan: "04"
subsystem: backend/migrations
tags: [alembic, migrations, sqlite, infra, gap-closure]
one_liner: "Populated empty initial Alembic migration with CREATE TABLE DDL for 4 tables and fixed test INSERT column names"

dependency_graph:
  requires: []
  provides: [working-alembic-migration-chain, fresh-db-setup]
  affects: [backend/alembic, backend/tests]

tech_stack:
  added: []
  patterns:
    - Alembic op.create_table() with sa.text() server_defaults for SQLite compatibility
    - Dependency-ordered CREATE TABLE (users -> sessions/transactions/emails)
    - Reverse-ordered DROP TABLE in downgrade (emails/transactions/sessions -> users)

key_files:
  modified:
    - backend/alembic/versions/7a9eaedc9937_initial_schema.py
    - backend/tests/test_migrations.py

decisions:
  - "payment_source excluded from initial migration — belongs only to 628c6541bc23_add_payment_source.py to avoid duplicate column on upgrade"
  - "server_default uses sa.text() literals for SQLite compatibility (0/1 for booleans, string literals for varchar defaults)"
  - "Both worktree and main-repo migration files updated — main repo holds untracked alembic.ini and venv needed to run tests"

metrics:
  duration: 10min
  completed_date: "2026-04-05"
  tasks_completed: 1
  files_modified: 2
---

# Phase 03 Plan 04: Fix Empty Initial Alembic Migration Summary

Populated the autogenerated-but-empty `7a9eaedc9937_initial_schema.py` migration with concrete `op.create_table()` DDL for all 4 tables (users, sessions, transactions, emails), and fixed a wrong-column-name INSERT in the migration test.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Populate initial_schema migration + fix test INSERT | 55408d4 | backend/alembic/versions/7a9eaedc9937_initial_schema.py, backend/tests/test_migrations.py |

## What Was Done

### Task 1: Populate initial_schema migration with CREATE TABLE DDL and fix test INSERT columns

**Part A — Migration file:**

`upgrade()` was replaced from `pass` to four `op.create_table()` calls in dependency order:
1. `users` — id, username, email, password_hash, totp_secret_encrypted, totp_enrolled, gmail_token_encrypted, is_active, created_at, updated_at + indexes
2. `sessions` — id, user_id (FK → users), session_token, expires_at, created_at, ip_address, user_agent + indexes
3. `transactions` — id, user_id (FK → users), transaction_date, amount, description, merchant, category, payment_method, notes, source, email_message_id, created_at, updated_at + indexes (NO payment_source — that is added by migration 628c6541bc23)
4. `emails` — id, user_id (FK → users), gmail_message_id, sender, subject, received_at, parse_status, parse_error, bank_name, created_at, delete_after + indexes

`downgrade()` was replaced from `pass` to `op.drop_table()` calls in reverse order: emails → transactions → sessions → users.

**Part B — Test fix:**

`test_payment_source_column_nullable` had a raw INSERT using wrong column names:
- Old (wrong): `(id, email, hashed_password, is_active, is_verified)`
- New (correct): `(id, username, email, password_hash, totp_enrolled, is_active)`

The old names matched a different schema; the actual User model uses `username` (NOT NULL), `password_hash` (not `hashed_password`), `totp_enrolled` (not `is_verified`).

## Verification Results

```
tests/test_migrations.py::test_alembic_upgrade_head PASSED
tests/test_migrations.py::test_payment_source_column_nullable PASSED
2 passed in 0.27s
```

## Deviations from Plan

### Auto-fixed Issues

None — plan executed exactly as written.

### Environment Note (not a deviation)

The git worktree at `.claude/worktrees/agent-a2405c66/backend/` is a partial checkout — it contains only git-tracked files, so it lacks `alembic.ini`, `venv`, `app/database.py`, and `alembic/env.py`. These are untracked local files that live only in the main repo's `backend/`. Tests were verified by running from `C:\Users\rahul\projects\expense-tracker\backend\` using that repo's infrastructure. Both repos received the same file updates.

## Known Stubs

None.

## Self-Check: PASSED

- `backend/alembic/versions/7a9eaedc9937_initial_schema.py` — contains 4 `op.create_table` calls, 4 `op.drop_table` calls, 0 `payment_source` references
- `backend/tests/test_migrations.py` — contains `password_hash`, does not contain `hashed_password`
- Commit `55408d4` exists in git log
- Both migration tests pass: 2 passed, 0 failed
